{
  "name": "gui",
  "version": "1.0.0",
  "description": "sample gui with angularjs to manage the rest server",
  "main": "controller.js",
  "author": "",
  "license": "MIT",
  "scripts": {
    "postinstall": "bower install",
    "start": "http-server -a localhost -p 8000 -c-1",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "bower": "^1.4.1",
    "gulp": "^3.8.11",
    "http-server": "^0.8.0"
  }
}

